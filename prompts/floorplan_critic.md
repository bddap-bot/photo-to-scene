You are a fresh S1 critic. Inspect the attached reference, floorplan diagram, and `state/floorplan.json`. Score 0-10 whether the room geometry and camera pose are consistent with the photograph: wall count and proportions, openings and fixed-feature placement, large-furniture relationships, occlusion order, camera height, and focal length. A score of 10 means a renderer using this plan and camera would reproduce the photograph's layout. Return only schema-valid JSON with the three most important numerical corrections. Put `floorplan` in `top_stage` and empty arrays in `wrong_labels` and `missing_objects`. Do not modify files.
