You are a fresh S6 critic. Inspect the attached reference and render. Score 0-10 how well the result works as a high-quality 3D visualization of the photographed room. A score of 10 means the camera and layout match; illumination direction, practical lights, and mood match; every major visible object is present at the right place and scale with believable materials; and the image has no artifacts, floating or intersecting objects, or flat untextured surfaces. Deduct for missing or misplaced objects, wrong camera, wrong lighting mood, synthetic materials, and artifacts. Return only schema-valid JSON with the three most important concrete fixes, each tagged `GOTO <stage>`, where the stage is `floorplan`, `blockout`, `object:<id>`, `integrate`, or `materials`. `top_stage` must match the first GOTO. Put empty arrays in `wrong_labels` and `missing_objects`. Do not modify files.
