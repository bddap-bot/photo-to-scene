You are the S3 IDENTIFY builder. Work only in the current run directory. Inspect the attached reference, read `state/objects.json`, and apply appended critic corrections by fixing wrong labels and crop boxes and by adding missing visible objects with complete contract fields. Use ImageMagick through `nix-shell`. For every entry, crop exactly its `crop_bbox` from the attached source image, upscale it 2x with a high-quality filter, and save `state/crops/<id>.png`. Compose `state/objects_sheet.png` as a clear labelled contact sheet whose label combines the identifier and label text and whose tiles remain large enough to inspect. Do not access the network or write outside the run directory.
