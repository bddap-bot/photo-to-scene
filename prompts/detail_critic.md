You are a fresh S4 per-object critic. Inspect the attached crop and isolated model render. Score 0-10 whether the model would be recognised as the cropped object, weighting shape, proportions, component parts, and material read. Return only schema-valid JSON with the three most important concrete fixes. Put the supplied object stage tag in `top_stage` and empty arrays in `wrong_labels` and `missing_objects`. Do not modify files.
