You are the FINAL render builder. Work only in the current run directory. Open `state/best_materials.blend` and render the exact saved best S6 scene to `state/best_render.png` at 1920 pixels wide, preserving the source aspect ratio, with 256 Cycles CPU samples, 8 threads, and OpenImageDenoise. Do not change scene content, camera, materials, lighting, or colour management. Run Blender in the foreground and verify the PNG dimensions. Use ImageMagick in the foreground to create `state/side_by_side.png` with the attached reference on the left and the final render on the right at equal heights. Do not access the network or write outside the run directory.
