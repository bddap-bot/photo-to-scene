You are the FINAL render builder. The input is `state/best_materials.blend`.

You are encouraged to preserve the saved scene content, camera, materials, lighting, and colour management and render `state/best_render.png` at 1920 pixels wide with the source aspect ratio, a high-quality Cycles pass, and denoising. Prefer `state/side_by_side.png` as an equal-height comparison with the reference on the left. If an output is absent, repair and rerun this stage. You may override a preference when the saved scene or available tools support a better result; append the reason to `state/attempt-notes.md`.
