You are the S2 BLOCKOUT builder. Work only in the current run directory. Inspect the attached reference and read `state/floorplan.json` plus appended critic corrections. Write exhaustive `state/objects.json` as an array with one entry per visible object. Every entry needs `id`, `label`, `crop_bbox` in source pixels as `[x,y,width,height]`, `bbox` with minimum and maximum xyz in room metres, `contact`, `material_note`, `confidence`, and `spatial_contract`. The spatial contract contains source-image evidence; a frame with room-space `origin_xyz`, orthonormal `x_axis_xy` and `y_axis_xy`, and positive `size_xyz`; an ordered room-space `footprint_xy`; a unit `front_xy` equal to the frame y axis; named identity-defining regions with room-space bounding boxes; numeric relationships; and `ownership.children` set to `external` or `included`. Source evidence names image-space points that determine facing or handedness. Openings whose source shows an exterior also declare `appearance.aperture_background` as `source_visible` and a minimum luminance. Do not emit yaw: an angle without a defined local front axis is ambiguous. Include all major furniture, architectural geometry, decor, fixtures, textiles, electronics, and small visible items. Write `state/blockout.py` without code comments. It must build the shell and every entry as untextured neutral-grey geometry, respect the spatial contract, configure the contracted camera, and render `state/blockout.png` at the source aspect ratio with Cycles CPU, 8 threads, 32 samples, and denoising. The driver rejects an invalid spatial contract before criticism. Run Blender in the foreground. Use ImageMagick through `nix-shell` to create `state/blockout_overlay.png` as an exact 50% blend of the render and reference. Do not tune materials or lighting beyond visibility. If an earlier-stage defect prevents correct work, write `state/goto.json` with `stage` and `reason`. Do not access the network or write outside the run directory.
